argon2-cffi==25.1.0
argon2-cffi-bindings==25.1.0
asgiref==3.9.1
cffi==1.17.1
Django==5.2.5
djangorestframework==3.16.0
gunicorn==23.0.0
Pillow==11.3.0
psycopg[binary]==3.2.10
pycparser==2.22
sqlparse==0.5.3

# Mobile-First / PWA (Sprint 8 - Mobile-First Architecture)
# Substitui o TailwindCSS via CDN por build local integrado ao Django.
# Pré-requisito: Node.js >= 18 e npm instalados na máquina de desenvolvimento.
# Setup: python manage.py tailwind init theme && python manage.py tailwind install
django-tailwind[reload]==3.8.0

# Testing (M7 — Polimento)
pytest==8.3.4
pytest-playwright==0.6.2
axe-playwright-python==0.1.5
